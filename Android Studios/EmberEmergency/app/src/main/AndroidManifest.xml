<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmberEmergency"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fire Simulation Activity -->
        <activity android:name=".FireSimulationActivity" />

        <!-- Resources Activity -->
        <activity android:name=".ResourcesActivity" />


        <!-- State and County Office Activity -->
        <activity android:name=".StateCountyActivity" />

        <!-- Legal Activity -->
        <activity android:name=".LegalActivity" />

        <!-- Food Activity -->
        <activity android:name=".FoodActivity" />

        <!-- Housing Activity -->
        <activity android:name=".HousingActivity" />

        <!-- Other Non-Profit Organizations Activity -->
        <activity android:name=".OtherNonProfitActivity" />

    </application>

</manifest>
